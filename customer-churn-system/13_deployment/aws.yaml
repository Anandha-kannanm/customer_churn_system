# AWS Elastic Beanstalk / ECS reference config
version: 1
application: customer-churn-system
environment:
  PYTHONPATH: /app
  PORT: 8000
container:
  image: churn-api:latest
  port: 8000
  command: uvicorn 6_api.app:app --host 0.0.0.0 --port 8000
